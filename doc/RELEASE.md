# `zalkera-devtools` 발행 절차

> **이 문서가 생긴 이유.** 레포 루트에서 `npx vsce publish` 를 부르면 이렇게 죽는다:
>
> ```
> ERROR  Missing vscode engine compatibility version.
>        ("engines": { "vscode": "<version>" } in package.json)
> ```
>
> 확장은 **npm 워크스페이스 멤버**(`packages/vscode`)라 `vsce` 가 **레포 루트를 패키지 루트로**
> 잡는다. 루트 `package.json` 은 모노레포 루트라 `engines` 가 없다. `scripts/package-vsix.mjs`
> 가 그 함정을 피하려고 워크스페이스가 없는 스테이징에서 포장하는데, 그 사실이 어디에도 안
> 적혀 있어 발행하는 사람이 매번 같은 자리에서 막혔다.

---

## 0. 언제 버전을 올리나

`packages/vscode/package.json` 의 `version` 이 마켓의 그것과 같으면 **마켓이 재발행을 거절한다.**
소스를 고쳤으면 반드시 올린다.

**등급**:

- **patch** — 고객이 보는 동작이 그대로이거나 결함만 사라지는 변경(성능·거짓 경고 제거·문면).
- **minor** — 고객이 보던 것이 달라지는 변경. 명령·설정·알림 문면이 바뀌거나, **검사기가 새로
  경고를 내는** 경우가 여기다. 진단은 편집기에 바로 뜨므로 「지금까지 안 뜨던 것이 뜬다」는
  고객에게는 동작 변경이다.

⚠ **버전 드리프트를 잡는 검사기가 없다.** `npm run verify` 의 검사기 어느 것도 `version` 을
마켓과 대조하지 않는다. 그래서 아래 §3 의 태그가 유일한 기록이다.

---

## 1. 굽기

```bash
cd zalkera-devtools
npm run package          # 버전은 이미 올려 둔 값으로. 올리면서 구우려면 package:patch / package:minor
```

`npm run verify`(시험 + 검사기 전량)를 먼저 돌리고, `dist/zalkera-devtools-<version>.vsix` 와
`shared/` 사본을 만든다. **`vsce` 는 이 스크립트 안에서만 부른다** — 직접 부르면 위 오류가 난다.

산출물이 이번 변경을 실제로 담았는지 확인한다:

```bash
unzip -p dist/zalkera-devtools-<version>.vsix extension/dist/extension.cjs | grep -c '<식별자>'
```

⚠ **비-ASCII 로 grep 하지 마라.** esbuild 는 문자열 리터럴의 한글을 `\uXXXX` **대문자 16진**으로
이스케이프한다(주석은 그대로 남는다). 한글로 찾으면 「수정이 번들에 없다」고 오보한다.
식별자·ASCII 패턴으로 찾을 것.

---

## 2. 발행

```bash
read -rs VSCE_PAT && export VSCE_PAT     # 비밀번호 관리자에서 붙여넣기
npx vsce publish --packagePath dist/zalkera-devtools-<version>.vsix
unset VSCE_PAT
```

**이미 구운 파일을 올린다.** `--packagePath` 없이 부르면 `vsce` 가 다시 포장하려 들면서 §0 의
오류가 난다.

⚠ **토큰을 명령줄에 두지 마라.** `--pat <값>` 은 `ps` 로 남이 읽고 셸 히스토리에도 남는다.
`read -rs` 는 에코도 히스토리도 없이 환경변수로만 넣는다.

■ **누가 부르는가 — 토큰을 가진 사람이 부른다**

토큰은 **디스크에 없다.** 비밀번호 관리자에 있고, 발행하는 그 순간 환경변수로만 들어왔다가
나간다. 그래서 이 문단의 규율은 문서에만 있는 것이 아니라 **기제로 받쳐진다.**

- **에이전트는 스스로 발행하지 못한다.** 그 셸에 `VSCE_PAT` 가 없고, 없으면 `vsce` 는 대화형
  프롬프트로 떨어져 무인 실행이 거기서 멈춘다. 「구웠으니 발행할까요」까지가 에이전트의 몫이다.
- **오너가 그 판을 가리켜 지시하고 토큰을 넣었을 때** 에이전트가 부를 수 있다. 이전 판에서
  받은 승인은 다음 판으로 이어지지 않는다.
- **되돌릴 수 없다.** 마켓은 같은 번호로 다시 못 올린다. 잘못 나가면 **판을 하나 더 쓰는 것**이
  유일한 수습이다. 그래서 3축 이수·게이트 초록·**나갈 트리 확정**이 이 문 앞에 선다
  (태그는 이 문 **뒤**다 — §3).

⚠ **`vsce login` 을 쓰지 마라.** 그것은 토큰을 `~/.vsce` 에 **평문으로** 적는다. 이 박스는
키링(`gnome-keyring`·`secret-tool`)이 없어 `vsce` 의 폴백이 **매번** 발동하고, 그러면 이 기계에서
도는 **모든** 에이전트가 같은 사용자로 그 파일을 읽는다. 되살아나면 `npm run package` 가 굽기
끝에 경고한다 — `npx vsce logout <퍼블리셔>` 로 지운다.

`VSCE_PAT` 가 저장분보다 **먼저** 쓰인다(`vsce` 의 `getPAT()` 가 `options.pat` 를 먼저 반환한다 —
소스 확인). 그래서 주입하는 쪽이 항상 이긴다.

---

## 2b. CLI 도 **같은 판으로** 발행한다 (오너)

```bash
npm run pack:cli                                  # verify → 번들 → npm pack → 검수
npm publish ./shared/zalkera-cli-<version>.tgz    # 오너만
```

⚠ **판을 올릴 때마다 둘 다 발행한다.** CLI 전용 게이트가 없다 — CLI 는 자기 판을 확장 판
자리에 실어 보내고(`packages/cli/src/context.ts` 의 `fetchHandshake(serverUrl, version(), …)`),
서버는 `min-extension-version` 하나로 판정한다. 그래서 두 판이 갈리면 한쪽이 게이트를 못 넘는다.

⚠ **다만 이것은 조건문이다 — 지금 갇히는 사람은 없다.** 상용 값이 `0.1.0` 이라 아무도 거절되지
않는다. 이 절이 막는 것은 **그 값을 올리는 날** 확장만 새 판이고 npm 최신은 옛 판인 상태다 —
그때 터미널 사용자가 받는 안내는 「업데이트하세요」인데 받을 새 판이 없다.

재현:

```bash
curl -s 'https://api.zalkera.com/api/devtools/handshake?extensionVersion=0.23.0' \
  | python3 -c 'import sys,json; print(json.load(sys.stdin)["data"]["minExtensionVersion"])'
```

⚠ **`minClientVersion` 은 이 축이 아니다.** 같은 응답에 있지만 그 값은 스토어프론트 소스가 써야
하는 **`@zalkera/client`(SDK)** 최소판이다(`DevtoolsProperties.kt` KDoc). CLI 와 무관하다 —
이름이 닮아 실제로 오독된 적이 있다. 그리고 응답은 `ApiResponse` 봉투라 **`data` 안**에 있다.

⚠ **순서 제약은 없다.** 확장은 CLI 를 **부르지 않는다**(`runtime.ts` 의 `cliPath` 는 읽는 자리가
0이다 — 그 경로를 쓰던 **로컬 stdio MCP 서버**를 0.23.0 에서 걷었다). 두 배송물이 런타임으로
안 엮이므로 어느 쪽을 먼저 내도 그 사이에 깐 사람이 깨지지 않는다.

⚠ **「소스를 다루는 권한」이 사라진 것이 아니다.** 걷은 것은 **그 권한으로 가는 경로 하나**다.
같은 권한은 원격 MCP 에 그대로 있고, 그쪽이 더 넓다:

| 축 | 도구 |
|---|---|
| 걷은 것 — 로컬 stdio | 도구 **넷**: `zalkera_status` · `zalkera_pull` · `zalkera_push` · `zalkera_publish` |
| 남은 것 — 원격 `source` 평면 | `read_source` · `edit_source` · **`publish`** · **`activate_revision`** · `get_site_config` · `posts` · `products` · `status` |

(서버 이름은 `zalkera`, `.mcp.json` 등록 키는 `zalkera-source` 였다 — 둘 다 도구가 아니다.)

`publish`·`activate_revision` 은 백엔드에서 `SITE_PUBLISH` 를 요구한다 — **방문자가 보는 사이트를
바꾸는 문**이다. 문이 둘이면 자격 검사도 문면도 두 벌이 되어 갈리므로 하나로 접은 것이고,
사용자가 할 수 있는 일은 그대로다(`doc/MANUAL.md` 의 「AI 연결이 여는 것」이 그것을 적는다).

`check-version-lockstep.mjs` 는 **두 판이 갈리지 않았는가**만 잰다. 서버 값이 얼마인지는 모른다.

⚠ **발행 계정 위생.** 이 이름은 `npm i -g` / `npx` 로 **남의 기계에서 실행되는 바이너리**다.
토큰 하나가 그 전부에 닿으므로, 발행 계정은 **2FA(publish 단계까지)** 를 켜고 **발행 전용
granular 토큰**을 쓴다. VSIX 는 마켓 서명이 있지만 npm 쪽에는 대응물이 없다 — 언젠가
CI 신뢰 발행(OIDC · `publishConfig.provenance`)으로 옮기는 것이 정석이고, 그 전까지는 계정 위생이
유일한 통제다.

⚠ **`npm pack` 을 직접 부르지 마라.** 그것은 지금 `dist/` 에 있는 것을 묻지 않고 담는다.
`tsc` 조각이 남아 있으면 번들 대신 그것이 실려 고객 기계에서 `ERR_MODULE_NOT_FOUND` 가 난다.
`pack-cli.mjs` 가 지우고·굽고·검수한다(자립 · shebang 1개 · 예상 밖 항목 0 · 판 축).

산출물이 실제로 도는지는 **설치해서** 본다 — 굽기만으로는 안 드러나는 갈래가 있다(실측: shebang
두 줄이면 포장은 성공하고 `node` 가 2행에서 `SyntaxError` 로 죽는다):

```bash
npm i -g ./shared/zalkera-cli-<version>.tgz && zalkera --version
```

---

## 2c. 이름 방어 스텁 — **한 번만** (오너)

```bash
npm run pack:guards
npm publish ./shared/guard-zalkera-devtools-0.0.1.tgz --access public
npm publish ./shared/guard-zalkera-cli-0.0.1.tgz --access public
npm deprecate zalkera-devtools "이 이름은 쓰이지 않습니다. 터미널 도구는 @zalkera/cli 입니다."
npm deprecate zalkera-cli      "이 이름은 쓰이지 않습니다. 터미널 도구는 @zalkera/cli 입니다."
```

`zalkera-devtools` 는 **우리 레포 이름**이자 마켓 확장 id 의 절반이라, 그 이름으로 손을 뻗는
사람이 생긴다. 먼저 등록한 쪽이 **설치 시점 코드 실행**을 얻고, 그것도 우리 도구가 refresh
토큰을 평문으로 두는 바로 그 기계에서다.

⚠ **판 축이 없다.** 이 스텁은 실행 표면이 0 이라(＝`bin`·의존·스크립트 없음) 내용이 안 바뀐다 —
한 번 내고 잊는다. `DECISIONS.md` 의 「별칭을 함께 내지 않는다」와 안 부딪히는 이유가 그것이다.

⚠ `@zalkera/*` 는 **스코프라 외부가 못 낸다.** 맨 이름 `zalkera` 도 npm 이 유사도로 거절한다
(우리에게도 거절했다). 그래서 지킬 것은 이 둘뿐이다.

---

## 3. 발행 **다음** — 태그를 찍는다

`dist/*.vsix` 는 gitignore 다. 태그를 안 찍으면 **어느 트리가 나갔는지 아무 데도 안 남는다.**
실제로 한 번 그랬다 — 같은 버전 번호로 커밋 열 개가 쌓여, 마켓의 그 버전이 무엇인지 레포
안에서 판별할 수 없었다.

```bash
BUNDLE=$(unzip -p dist/zalkera-devtools-<version>.vsix extension/dist/extension.cjs | sha256sum | cut -d' ' -f1)
VSIX=$(sha256sum dist/zalkera-devtools-<version>.vsix | cut -d' ' -f1)
git tag -a v<version> -m "발행본 sha256: vsix $VSIX · bundle $BUNDLE
재현(번들): npm run package && unzip -p dist/zalkera-devtools-<version>.vsix extension/dist/extension.cjs | sha256sum" HEAD
git push origin v<version>
```

두 sha 의 쓰임이 다르다.

- **번들 sha** 는 **재현된다**. esbuild 산출물이 결정론적이라 같은 트리에서 다시 구우면 같다.
  나중에 「마켓의 그 판이 어느 트리냐」를 되짚는 것은 이쪽이다.
- **vsix sha** 는 **재현되지 않는다.** 스테이징에서 `node_modules` 를 복사할 때 zip 항목 **순서**가
  실행마다 달라진다(내용은 같다). 이 값은 마켓이 알려 주는 `VsixSha256` 과 대조하는
  **발행본 식별자**로만 쓴다.

---

## 4. 갱신 안내는 **비파괴 한 줄**로만 적는다

배송 문서(README·help)가 사용자에게 시키는 갱신 명령은 이것 하나다.

```bash
code --install-extension zalkera.zalkera-devtools --force
```

**지우라고 하지 마라.** `uninstall` 과 `install` 을 한 펜스에 담으면, 통째로 복붙한 사람이
2줄에서 네트워크·마켓 장애를 만났을 때 **확장이 사라진 채로 남는다.** 그 사람에게는 `.vsix`
파일도 없다.

⚠ **「파일로 깔면 갱신을 못 받는다」를 단정하지 마라.** VS Code 1.134.0 번들을 읽어 확인한
것은 둘이다 — VSIX 설치가 끝나면 `this.source instanceof …&&this.updateMetadata(...)` 가 돌고,
갱신 대상 선별은 `a.identifier.uuid&&n.push(...)` 로 uuid 만 본다. 즉 마켓에 올라간 뒤에 파일로
깐 사람은 갱신 대상에 들어간다. 종전 문면은 그들에게 필요 없는 수술을 시켰다.

**이 박스에서는 실동작을 재현하지 못했다** — 여기 VS Code Server 에는 확장이 하나도 안 깔려
있다. 데스크톱에서 확인하기 전에는 어느 방향으로도 단정하지 말 것.

포크(Cursor·Windsurf)는 다르다. OpenVSX 에 `zalkera` 네임스페이스가 없어(실측 404) 마켓 경로로는
설치가 안 되고, 자동 갱신도 오지 않는다. 그 사실은 배송 문서에 적혀 있어야 한다.

강제로 올려야 하면 `minExtensionVersion`(백엔드 설정)을 올리는 길이 있다. 그러면 그 미만 판은
알림이 아니라 **하던 일이 멈춘다** — 사용자 작업을 끊는 것이라 오너 판단이다.

## 5. 변경 기록에 무엇을 적나

`packages/vscode/CHANGELOG.md` 는 **고객이 마켓에서 읽는 문서**다. 우리 규율을 설명하는 자리가
아니다 — 방침 문장은 여기 적고, 거기에는 결과만 적는다.

- 보안 수정은 **무엇이 고쳐졌는지까지만.** 재현 조건·크기·형상은 적지 않는다 — 아직 옛 판을
  쓰는 사람이 그대로 노출된다.
- 그렇다고 **빼지도 않는다.** 「올려야 할 이유」가 안 보이면 그 사람은 옛 판에 남는다.
  「…를 고쳤습니다. 그 밖에 안전 관점의 수정이 여럿 있습니다」 정도가 두 요구를 다 만족한다.

## 6. 체크리스트

```
[ ] packages/vscode/package.json 의 version 을 올렸다
[ ] packages/vscode/CHANGELOG.md 에 이번 판 항목을 적었다 (VSIX 안에 실려 마켓에 그대로 뜬다)
[ ] **배송 문서 넷이 이번 변경을 담았다** — `packages/vscode/README.md`(마켓 개요 탭) ·
    `doc/MANUAL.md`(고객 PDF) · `packages/vscode/media/help.md` · `packages/cli/README.md`(npm 개요 탭). 고객이 보는 동작이 바뀌었는데
    이 셋이 그대로면 **변경 기록에만 있고 설명하는 자리가 없다**
[ ] npm run package  (verify 통과 + vsix 생성)
[ ] 번들에 이번 변경이 들어갔는지 확인 — **문자열은 ASCII 로 escape 되어 실린다**(주석은 원문).
    한글로 grep 하면 없는 것처럼 보이므로 **대문자 hex** 로 찾는다:
    `python3 -c 'import io,sys; b=io.open(sys.argv[1],encoding="utf-8").read();
     print(b.count("".join(c if ord(c)<128 else "\\u%04X"%ord(c) for c in sys.argv[2])))' \
     packages/vscode/dist/extension.cjs '이미 게시됐습니다'`
[ ] npx vsce publish --packagePath dist/…vsix   (오너 지시가 있을 때 — §2)
[ ] **CLI 도 같은 판으로** — `npm run pack:cli` → `npm publish ./shared/zalkera-cli-…tgz` (오너)
    ⚠ 서버 최소판 게이트가 **하나**다. 한쪽만 올리면 다른 쪽 사용자가 「업데이트하세요」를 받는데
    받을 새 판이 없다. `npm run package` 배너도 이 두 줄을 같이 찍는다
[ ] v<version> 태그를 sha256 과 함께 찍고 push — **sha 는 마지막 포장 뒤에 파일에서 다시 읽는다.**
    포장을 한 번 더 돌리면 vsix sha 가 바뀌므로(zip 항목 순서), 먼저 적어 둔 값은 무효다
[ ] 파일로 깔아 둔 사람이 있으면 마켓 경로로 다시 깔라고 안내 (§4)
[ ] §7 표에 이 판의 심의 상태를 적었다 — 「미이수」면 그렇게 적는다
```

## 7. Fable 심의 이수 현황

**표준은 3단이다: Fable 설계 → Opus 검토·구현 → Fable 심의(3축: 기능·보안·성능).** Fable 한도가
차면 심의를 Opus 로 돌리는데, 그것은 **대체가 아니라 임시**다 — 설계자가 자기 설계대로 됐는지
본 적이 없는 판이 쌓이므로 어느 것이 미이수인지 여기 적는다. 안 적으면 나중에 몰아서 받을
목록을 만들 수 없다.

⚠ **인접 심의를 「함께 봤다」로 끌어와 줄을 지우지 마라.** 그 판단이 곧 심의의 몫이고, 이 절이 있는 이유가 그것이다 — 줄을 지우면 몰아서 받을 목록이 사라진다. 겹치는 심의가 실제로 그 면을 봤다면 **무엇을 봤는지 적어** 판정하고, 안 봤으면 남긴다.

⚠ **미이수는 「결함이 있다」는 뜻이 아니다.** Opus 3축과 검사기·시험을 지난 판이다. 다만
「설계자가 본 적 없다」는 사실이 기록으로 남아야 한다.

### 설계자 심의 이수 — 0.21.0 · 0.20.0 · T3 설계면 · 이름 스텁

밀려 있던 넷을 설계자가 함께 받았다. **판정은 넷 다 「조건부 이수」** 이고, 조건 넷은 같은 트랜치에서
이행했다. 🔴 는 0 이다.

| 받은 것 | 설계자 판정 | 이행한 조건 |
|---|---|---|
| **0.21.0** T4 — SDK 없이 stdio 직접 구현 · `zalkera_` 접두 · CLI 동봉 | 조건부 이수 · 🟠1 | stdio 프레이밍(오버플로·재동기화)에 그물이 **비어 있었다**. 재동기화를 「버퍼 통째 비우기」로 되돌려도, 상한 가드를 지워도 21개가 전건 초록이었다 → 시험 둘 |
| **0.20.0** T3 2회전 반영분 + 사이트 전환 경로 표시 | 조건부 이수 · 🟠1 | 버리기 사전 재검의 「못 읽음 → 멈춤」 접기가 무시험. 배선 앵커가 세대 비교 **부분문자열**만 핀해 접는 방향을 못 본다 → 시험 1 |
| **memo184 T3 설계면** — `errors[].field` 분기 · 되돌리기 착지 판 판독 | 조건부 이수 · 🟠1 | 동의 이음 판정이 **차단목록**이라 「모르는 표식」이 위험 쪽으로 접혔다 → 술어를 둘로 가름 |
| **이름 방어 스텁** | 조건부 이수 · 🔴🟠 0 | 스텁 자체는 무해 확정(회수 불요). 조건은 아래 판단 기준을 남기는 것 |

설계 결정 셋은 **전부 수용**됐다. 특히 CLI 동봉의 근거(「`npm exec` 는 폴더의 `node_modules` 를 버전만
맞으면 우선한다」)는 설계자가 격리 폴더에서 **재현했다** — `--offline` 로도 폴더 코드가 실행됐고,
버전 문자열은 공격자가 정하므로 **판 고정으로는 못 막는다**. 동봉+절대경로가 그 구멍을 닫는다.

기각: 옛 판 협상 시의 배치 거절(🟡 · 배치를 보내는 실사용 클라이언트가 없다) · `zalkera_` 이중 접두(🟡).

### 되돌릴 수 없는 외부 부수효과에 대한 판단 기준

npm 발행·마켓·DNS·상용처럼 **되돌릴 수 없는** 것은 「실행 표면이 0 이라 무해하다」는 구현자
자기판단으로 내보내지 않는다 — **그 무해성 판단 자체가 심의의 대상**이기 때문이다.

다만 심의의 **깊이는 산출물에 비례시킨다.** 무실행을 주장하는 아티팩트라면 심의는 기계적 목록으로
끝난다: 탈볼 전수 열거 · `bin`/`main`/`scripts`/`dependencies` 부재 단언 · `--foreground-scripts`
설치 실측 · `.bin` 항목 확인. 몇 분이다.

그러므로 **경쟁·긴급은 이 목록을 발행 직전으로 앞당길 이유일 뿐 생략할 이유가 못 된다.** 부득이
먼저 나갔다면 순서는 이번과 같다 — 즉시 원장에 건너뜀을 적고, npm 72시간 회수 창이 닫히기 전에
심의를 세운다. 결과가 무해로 나온 것이 건너뜀을 소급 정당화하지는 않는다.

⚠ **그 사이에 나가는 판은 이 표에 「Opus 단독」으로 적는다.** 적지 않으면 몰아서 받을 목록이
만들어지지 않는다 — 이 절이 있는 이유가 그것이다.

| 판 | 커밋 | 받은 심의 | Fable |
|---|---|---|---|
| **0.24.0** | (이 판) | 0.23.0 반려 조건 이행 · **Opus 3축** 3회전 | **미이수** — ⑴ `RELEASE.md` 의 전제를 사실로(걷은 것은 **경로 하나**이고 같은 권한은 원격 MCP 에 그대로 있다 — 양쪽 도구 목록을 실측으로 실었다) ⑵ 그 위에 선 문면 정정(`MANUAL.md` 는 원래 정확했다) ⑶ `isOurEntry` 가 **주소를 안 봐서** oauth 짝만 갖춘 남의 http 항목(Linear·Sentry)을 우리 것으로 오인해 `Authorization` 헤더를 지웠다 — **변이 없이 재현됐다**. 오리진 일치를 요구하게 고쳤다. 2회전이 조건 둘을 걸었고(근접 오리진 픽스처 · 거짓 하중 주장) 3회전이 이행을 확인했다: 느슨해지는 변이 **다섯**(`===`→`startsWith`·`includes` · `.origin`→`.hostname`·`.host` · try/catch 제거)이 전부 red 이고, 픽스처 3줄을 걷어내면 **5/5 초록으로 되돌아간다**(반사실 실측). 재현: 각 변이 후 `npm run test -w @zalkera/devtools-core`. ⚠ **판 등급이 patch 에서 minor 로 올라갔다** — 지금까지 안 뜨던 거절이 뜬다(§0). `0.23.1` 은 발행된 적이 없다 |
| **0.23.0** | `5ffb824` 까지 | 「소스 다루게 하기」 철거 · 설계 없음(오너 판단) · **Opus 구현** + **Opus 3축** 2회전 | **미이수** — 1회전이 **반려**했고 그 조건을 0.23.1 이 이행했다. 반려 사유: ⑴ 전제가 거짓 ⑵ 배송 문면 셋이 그 거짓 위에 섰다 ⑶ `isOurEntry` stdio 갈래가 무그물 |
| **0.22.0** | `81d6c25`·`00290bb` | 설계자 심의 조건 이행(동의 이음 허용목록·프레이밍 그물·취소 전환) + **Fable 3축** 3회전 + **발행 전 심의** | **이수** — 발행 전 심의가 🔴 1(변경 기록의 거짓 약속 **세 번째 재발**)과 🟠 4(새 절대문·실물 없는 정형문·배송 문서 둘의 옛 절대문·lock 미동기)를 잡아 이행했다 |
| 0.22.0 이후 | `e805237` | **없음** — 원장 정정 커밋(0.20.2 를 「이수」로 올린 것을 되돌린 문서 변경) | **해당 없음** — 코드 변경 0. 다만 이 표의 규율상 「3축을 안 받았다」를 적어 둔다 |
| 0.22.0 판 올림 | `8185c4b` | **한 사람이 3축을 겸했다**(별도 축 심의자 없음) | **부분** — 판 올림 + 배송 문면 정리라 새 실행 표면이 0 이지만, 「세 축을 각각 받았다」고 말할 수 없다. 다음 판 심의가 이 판의 문면을 함께 본다 |
| **0.21.0** | `6eb7db4` | memo184 T4(로컬 MCP 4도구 · `preview`) + 「AI 연결」 묶음 · 설계(**Fable**, memo184 §2.6) + **Opus 구현** + **Opus 4회전** + **Fable 3축** | **이수** — 조건 1(프레이밍 그물)을 이행했다. 매 회전이 🔴 를 냈고 **둘은 직전 회전 반영이 넣은 회귀**였다(스칼라 JSON 이 서버를 죽임 · 동봉 Node 가 Electron 으로 뜸). 4회전에서 새 결함 0 |
| **0.20.2** | `c96f78b` | CLI 를 npm `@zalkera/cli` 로 내는 길 · 설계 없음 · **Opus 3축 2회전** | **이수** — 설계자가 발행 경로·패키지 경계·스코프를 따로 봤다. 🟠 1(`pack:cli` 가 `shared/*.tgz` 를 전량 소거)은 이후 `83b88d7` 이 stem 접두로 좁혀 **이미 닫혀 있었다**(실물 재현 확인). 발행 실물 4파일·의존 0·라이프사이클 스크립트 0이고, **태그 트리에서 구운 번들 sha256 이 npm 발행본과 일치**한다 |
| **0.20.1** | `9a78ccb` | 확장 번들 동기화(핸드셰이크 안내 · 오류 코드) · 고객 가시 변경 0 · **Opus 검토** | **이수** — 「고객 가시 변경 0」이 **재현 가능한 사실**로 성립했다(재빌드 sha256 == 마켓 발행본 · 번들 diff 2줄 · 호출 두 자리가 `upgradeHow` 미전달). 조건 1(그 네 자리 무그물 — 변이 4종 생존)을 이행했다 |
| **0.20.0** | `d4dec76` | ⑴ memo184 T3(CLI 발행·되돌리기·버리기) — 설계(**Fable**, memo184) + **Opus 구현** + **Fable 3축**(1·2회전) + **Opus 조건 이행 확인**(3회전) · ⑵ 사이트 전환 경로 표시 — **Opus 단독**(설계 없음 · Opus 3축) | **이수** — 설계자가 2회전 반영분의 회귀(변이 14종 중 12 적색)와 ⑵ 의 형상을 함께 봤다. 조건 1(재검 접기 그물)을 이행했다 |
| **0.19.0** | `b550030` | 사이트 전환의 판 차이 고지 · 설계(**Fable**) + 오너 결정(문면·파괴 동사는 안 올림) + **Opus 구현** + **Fable 3축** | **이수** — 3축이 🟠 하나(표시 쪽 배선 무방비 — 변이 넷이 전건 초록으로 통과)를 잡았고 이행 뒤 2회전에서 11/11 red 로 확인했다. 설계 정본은 `doc/DESIGN-elsewhere-drift.md` |
| **0.18.0** | `587ae24` | 「서버 판으로 교체」 · 설계(**Fable**) + 오너 결정(이름·잔재 알림 수위) + **Opus 검토·구현** + **Fable 3축** | **이수** — 표준 3단을 그대로 밟았다. 기능 조건부(🟠 3)를 이행했고 보안·성능은 승인. 설계 정본은 `doc/DESIGN-server-replace.md` |
| **0.17.0** | `7537172` | 발행 취소 + 사이드바 줄 문면 · 설계 심의 → **Opus 3축** → **Fable 조건 이행 확인** | **이수** — 설계자 3축. 🔴 1(취소 전환이 종류를 안 갈라 `SERVER_UNREACHABLE` 까지 삼켜, 결과를 모르는 실패를 「만들어지지 않았습니다」로 **단정**했다)을 이행했다. 등가 변이 3종 red 는 오늘도 재현된다 |
| **0.16.0** | `6a9251f` | 명세 A 하행 + 동시 업로드 최소 방어(§B) · 설계(Fable) + **Opus 3축** + **Fable 3축**(A·B 각각) | **이수** — 세 축·설계 전부. 반려 2회·조건부 4회를 이행해 낸 판이다 |
| 0.16.0 에 실림 | `56e4343` | 뒤늦은 **Fable 3축** | **이수** — 「로그 2줄」 기재가 참으로 확인됐다(분기·오류 처리·행위 변경 0). 🔴🟠 0. 남은 🟡 둘은 기각: ⑴ 커밋 메시지의 「실패·타임아웃·취소·추월을 전부 log()」는 거짓이다(`timeout`·`gone` 은 알림만) — 다음 판에서 두 줄로 닫을 일이지 회수 사유가 아니다 ⑵ 패널 줄이 알림과 달리 반영 지연 헤지를 안 싣는다 |
| 0.15.0 | (따로 발행하지 않고 **0.16.0 에 흡수**) | 백엔드 명세 A 하행 · Opus 변이 검증(판정 3종·배선 2종) | **이수** — 0.16.0 의 Fable 3축이 이 내용을 함께 봤다 |
| 0.14.1 | `ed7bceb`·`cb5726e` | 뒤늦은 **Fable 3축** + 조건 이행 | **이수** — 「새로 여는 면이 없다」는 표면 기준으로 참이었다. 다만 커밋이 「유일한 가드」로 자임한 배선이 **약속의 절반만** 지켰다: 앵커 줄을 두고 `await tellDraftBlocked(...)` 만 지운 변이가 배선 134자리·시험 900+ 전건 초록으로 생존했다(동의까지 받아 놓고 **침묵 return**). 조건 = 재시도 안내를 앵커로 등재 — 이행 후 그 변이가 적색이 되는 것을 확인했다. 「보안 축 지적」(로그아웃 세대) 이행분은 `ed7bceb` 가 아니라 `cb5726e` 라 커밋 칸에 병기한다 |
| **0.14.0** | `bf310e2` + 심의 반영 5 | 설계(Fable) + **Opus 3축** + **Fable 3축**(기능·보안·성능 전부 통과) | **이수** |
| **0.13.1** | `2927e54`·`d04ac10` | **Fable 3축**(기능·보안 조건부→이행 · 성능 통과) | **이수** — 이 판이 0.13.0 트랜치의 Fable 조건을 싣는다 |
| 0.13.0 | `009a1ad`·`29f9d99`·`9fb2e01` + 성능 반영 | 설계(Fable 2판) + Opus 3축 + **Fable 소급 심의** | **이수(소급)** — 고객 급건으로 Fable 앞서 발행. 그 트랜치의 Fable 3축은 뒤이어 돌아 조건이 0.13.1 에 실렸고, 2026-08-27 소급 심의가 세 판을 함께 훑어 🔴 0 · 🟠 1(`runElsewhere` 재확증)을 냈다 — 그 하나는 같은 날 닫혔다 |
| 0.11.3 · 0.12.0 | `63e5f39`·`bc12628` + 심의 반영분 | 설계 심의(초안 반려·대안 채택) + 3축(기능·보안·성능) | **이수** |
| 0.11.2 | `2ba180e` | Opus 자기 검수 + **Fable 소급 심의** | **이수(소급)** — 실변경은 화면 이름 한 줄이었고, 그때 실린 「지금 켜져 있는 버전으로 되돌리면 됩니다」의 거짓 문면은 0.14.1 에서 이미 정정됐다 |
| T5b(0.11.2 에 실림) | `6eea8eb` | Opus 3축 + **Fable 뒤늦은 3축**(조건부 → 0.14.1 에 이행) | **이수** |
| 0.11.1 | `412ae54` | Opus 단독 + **Fable 소급 심의** | **이수(소급)** — 순수 문면 판(라벨·안내 문구)이고 동작 변경 0 임을 커밋 범위로 확인했다 |
| 0.11.0 | `94177cc` | 4회전(1회전 3축 → **2회전 설계자** → 3·4회전 조건 이행) | 이수 |

⚠ **태그가 빠진 판이 있다: `v0.15.0`·`v0.16.0`.** §3 이 「태그가 유일한 기록」이라 적어 둔 그 자리가
비어 있다. `v0.14.1` 다음 태그가 `v0.17.0` 이라, 그 사이에 나간 트리는 커밋으로만 되짚을 수 있다
(0.15.0 은 따로 발행하지 않았고, 0.16.0 은 `6a9251f` 다). 발행본 sha 는 남아 있지 않다.

### 확장보다 **서버가 먼저** 나가야 하는 판이 있다

올리기 문의 계약을 만지는 판은 **백엔드가 먼저 상용에 있어야** 한다. 조건이 둘이고, 이 규율은
둘째를 한 번 놓쳐서 생겼다.

⑴ **필드를 아는 서버여야 한다.** HTTP 역직렬화가 미지 필드를 관용하므로, 모르는 서버는
   `baseRevisionNo` 를 400 이 아니라 **조용히 무시**한다. 그러면 표식 있는 폴더도 고지 없이
   무방어가 되어 **「조용히 무보호」가 전 사용자에게 재현된다.**

⑵ **탈출구가 서버에 있어야 한다.** 확인이 「재시도 가능한 거절」을 냈을 때 서버가 zip 을
   **보존**해야 사람이 「그대로 올리기」를 누를 수 있다. 지우는 서버 위에서는 그 단추가
   **없는 키**를 찾아 매 시도 실패한다 — **탈출구가 죽은 채로 방어만 켜지는 것이 최악**이다.

⚠ **역순은 무해하지만 그 반대는 아니다.** 구 확장은 새 서버에 필드를 안 보내므로 그냥 종전대로
   돈다. 반대로 새 확장이 구 서버를 만나면 위 둘이 다 깨진다.

**대조하는 법**: 방어가 켜지는 판을 굽기 전에, 걸리는 백엔드 커밋이 **상용에 반영된 머지**의
조상인지 본다 — `git -C <backend> merge-base --is-ancestor <커밋> <배포된 커밋>`.

- T5b 는 백엔드 `doc/design/memos/183-nondev-source-edit.md` 의 미이수 표에도 같은 사유로 올라
  있다(그 표의 T5 행이 `devtools 6eea8eb` 를 든다). **두 곳이 같은 사실을 말해야 한다.**
- Fable 이 볼 것은 재심의가 아니라 ⑴ Opus 심의 반영이 설계 의도와 맞는가 ⑵ Opus 가 **자기
  구현을 자기가 심의해서** 못 본 것이다.
