# 잘커라 확장 사용 매뉴얼

VS Code 에서 사이트 소스를 받아 고치고, 미리 보고, 올리는 도구입니다.
확장 안에도 도움말이 있습니다(`잘커라: 도움말`) — 그쪽은 **사전**이고 이 문서는 **작업 순서**입니다.

**이 문서의 자리.** 잘커라 확장에 관한 문서는 셋이고 역할이 다릅니다.

| 문서 | 무엇 | 어디 |
| --- | --- | --- |
| **이 매뉴얼** | 처음 한 번 · 매일 · 주의 사항 · 실제 순서 · git 과 함께 쓰기 · 잘 안 될 때 | 잘커라가 드리는 PDF · 확장 레포 `doc/MANUAL.md` |
| **도움말**(사전) | 명령·화면·파일 하나하나가 무엇인지 | 확장 안 `잘커라: 도움말` · 웹 <https://zalkera.github.io/zalkera-devtools/> |
| **마켓플레이스 소개** | 무엇을 하는 확장인지 한 장 | VS Code 확장 탭 |

같은 이야기가 둘에 겹쳐 있으면 **도움말이 낱말의 뜻**, **이 문서가 순서**입니다.

**목차**

- 1. 처음 한 번
- 2. 매일 쓰는 것들 — 사이드바 「버전」 읽는 법
- 3. 주의 사항 — 되돌릴 수 없는 것 · 조용히 어긋나는 것 · 미리보기 · AI 연결
- 4. 실제 시나리오 A~F
- 5. git · GitHub 와 함께 쓰기 (개발자·대행사)
- 6. 잘 안 될 때
- 부록 A. 명령 전체 표 · 부록 B. 확장이 폴더에 쓰는 파일 · 부록 C. 설정

---

## 1. 처음 한 번

```bash
code --install-extension zalkera.zalkera-devtools --force
```

깔면 왼쪽에 잘커라 아이콘이 생깁니다. **로그인** → **사이트 선택** 순으로 누르십시오.
맡으신 사이트가 여럿이면 그다음부터 그 자리가 **사이트 전환**으로 바뀝니다.
그다음부터는 사이드바가 지금 할 수 있는 것을 보여 줍니다.

> **Node.js 는 따로 까시지 않아도 됩니다.** 없으면 없는 대로 미리보기가 뜹니다.
> ⚠ 오히려 **낡은 Node.js 가 깔려 있으면 안 뜰 수 있습니다** — 아래 §5 를 보십시오.

## 2. 매일 쓰는 것들

| 하려는 일                              | 누르는 것             | 묶음      |
| -------------------------------------- | --------------------- | --------- |
| 배포 중인 소스를 **폴더로**            | **소스 다운로드**     | 내려받기  |
| 배포 중인 소스를 **파일 하나로**       | **소스 zip 다운로드** | 내려받기  |
| 예제를 파일 하나로                     | **예제 zip 다운로드** | 내려받기  |
| 이 창을 **다른 폴더로** 옮김           | **작업 폴더 변경**    | 작업 폴더 |
| 받은 zip 으로 **새로** 시작            | **zip 으로 시작**     | 작업 폴더 |
| 받은 zip 으로 **지금 폴더를 갈아 끼움**| **zip 으로 교체**     | 작업 폴더 |
| **서버 판으로** 지금 폴더를 갈아 끼움  | **서버 판으로 교체**  | 작업 폴더 |
| 화면 확인                              | **미리보기 시작**     | 미리보기  |
| 올리기 전 점검 · 올리기                | **배포 전 검사** · **새 버전 배포** | 배포 |
| AI 에게 **내 데이터**를 열어 줌         | **내 데이터 보여 주기** | AI 연결 |

「내려받기」 셋은 파일로 받거나 **빈 폴더**로 갑니다. 소스가 들어 있는 폴더는 건드리지 않고
옆에 새 폴더를 만들어 드립니다. 빈 폴더를 열어 두셨다면 **그 폴더에 받으시겠는지 먼저
여쭙습니다.**

소스가 들어 있는 폴더를 **덮어쓰는 것**은 「작업 폴더」의 **zip 으로 교체**와
**서버 판으로 교체** 둘입니다. 재료만 다릅니다 — 받으신 zip 이냐, 서버에 켜져 있는 판이냐.

> 「소스 다운로드」와 「소스 zip 다운로드」는 **같은 판**을 받습니다. 다른 것은 결과의 모양뿐입니다 —
> 앞은 바로 고칠 수 있는 폴더, 뒤는 남에게 넘기거나 보관할 파일 하나입니다.

명령 팔레트는 `Ctrl/Cmd + Shift + P` 입니다. 「zalkera」로 검색하면 전부 나옵니다.

**「새 버전 배포」를 누르면 손님이 보는 사이트가 바뀝니다.** 누르기 전에 「미리보기 시작」으로
확인해 주십시오. 잘못 올리셨으면 「버전 전환」에서 이전 판을 고르시면 그때로 돌아갑니다.

올리는 중에 **그만두실 수 있습니다.** 보내는 도중에 멈추시면 새 버전은 만들어지지 않습니다.
서버가 확인을 시작한 뒤에 누르시면 그 버전이 만들어졌을 수 있고, 그때는 기다리기만 그만두는
것입니다. 결과를 못 받았으면 단정하지 않고 연결 오류로 알려 드립니다.

### 지금 폴더가 서버와 같은지 — 사이드바 맨 위 「버전」

```
▾ 버전 · 일치
    서버 — 969a61e0 / 버전 4
    로컬 — 969a61e0
```

- **앞의 여덟 글자**(화면은 이것을 「지문」이라 부릅니다)가 그 판에 담긴 소스를 가리킵니다. 두 줄이 같으면 같은 소스입니다.
- **`버전 4`** 는 올릴 때마다 1씩 느는 번호입니다. **언제** 올렸는지만 말합니다 — 되돌리시면
  번호는 새것인데 여덟 글자는 그대로입니다. 게시 안내와 콘솔 버전 목록이 말하는 번호와
  같은 것입니다.
- 로컬 줄에는 **자기 번호가 없습니다.** 번호는 올릴 때 서버가 붙여 주는 것이라 아직 올리지
  않은 폴더는 가질 수 없습니다. 다만 이 폴더가 **서버에 켜진 것과 다른** 판과 같은 내용이면
  그 판의 번호를 빌려 `4번 내용 그대로` 처럼 적습니다.

**「버전」 옆에 결론이 한 낱말로 적힙니다.**

| 낱말 | 뜻 |
| --- | --- |
| 일치 | 같은 소스입니다. 지금 올리셔도 사이트는 안 바뀝니다 |
| 수정 중 | 마지막으로 맞추신 뒤 이 폴더를 고치셨습니다 |
| 서버가 더 최신 | 서버에 켜진 판이 이 폴더 내용보다 버전 목록에 나중에 나타났습니다 |
| 로컬이 더 최신 | 이 폴더 내용이 서버에 켜진 판보다 버전 목록에 나중에 나타났습니다 |
| 다름 | 두 소스가 다릅니다. **어느 쪽이 나중인지는 알 수 없습니다** |
| 확인 필요 | 아무것도 안 고치셨는데 서버와 다릅니다. 도구 쪽 사정일 수 있습니다 |
| 대조 불가 | 서버 판에 지문이 없어(「지문 없음」) 견줄 수가 없습니다 |
| 확인 중 | 이 폴더를 다시 세고 있습니다 |

> **「나중」은 올리신 순서가 아니라 버전 목록에 그 내용이 처음 나타난 자리입니다.** 되돌리시면
> 큰 번호에 옛 내용이 들어가기 때문입니다. 그 경우에는 서버 줄이 `버전 5 · 3번 내용 그대로`
> 처럼 적힙니다 — 5번을 3번으로 «빌드했다»가 아니라 **5번에 담긴 것이 3번 때 올리신 것과
> 같다**는 뜻입니다. 되돌리셨을 때 그렇게 되고, **같은 소스를 한 번 더 올리셔도** 그렇게 됩니다 —
> 그래서 「되돌렸다」가 아니라 「내용이 같다」로 적습니다.

어느 쪽으로 맞추실지는 사장님이 정하십니다 — 이 폴더 것을 올리시려면 **새 버전 배포**,
서버 것을 가져오시려면 **서버 판으로 교체** 입니다.

## 3. 주의 사항

### 되돌릴 수 없는 것 — 둘입니다

**`zip 으로 교체`**·**`서버 판으로 교체`** 는 지금 폴더의 내용을 지웁니다. 확인 창이 한 번 뜨고,
거기서 무엇을 남기는지 보여 드립니다.

**`서버 판으로 교체`**의 확인 창에는 **「zip 으로 내보내기 먼저」** 단추가 있습니다 — 지금 폴더를
챙겨 두고 싶으실 때 누르시면 내보내기로 가고, 갈아 끼우지는 않습니다.
⚠ **`zip 으로 교체`에는 그 단추가 없습니다.** 챙겨 두시려면 **먼저** 「zip 으로 내보내기」를
누르신 뒤에 교체하십시오.

**`서버 판으로 교체`**에서, 이 폴더가 **이미 서버와 같은 버전으로 표시돼** 있으면 확인 창의 첫 줄이 그
사실을 말합니다. 막지는 않습니다 — 폴더가 망가져 같은 판을 새로 받을 때는 그대로 진행하면 됩니다.

- **그대로 두는 것**: `.git` · `.env`·`.env.local` 같은 실제 환경 파일 · `.vscode` · `.idea` ·
  `.ssh` · `.aws` · `.npmrc` · `.mcp.json` · `.claude` · 인증서 파일
- **`.env.example` 류는 남기지 않습니다** — 소스의 일부라 받으신 zip 의 것으로 바뀝니다
- **지우는 것**: 그 밖의 모든 파일. `node_modules`·`.next` 같은 다시 만들어지는 것도 지웁니다
- **폴더 바로 아래만 지킵니다** — 하위 폴더 안의 `.env.local` 은 못 지킵니다

> **로컬에서 직접 고치신 것이 있으면 갱신 전에 빼 두십시오.** 없어집니다.
> 지금 상태를 통째로 보관하시려면 **zip 으로 내보내기** 를 먼저 쓰십시오.

갈아 끼우다 **실패로 멈추면** 원래 소스로 되돌립니다. 되돌리기까지 실패하는 드문 경우에는
**원래 소스가 어디 있는지** 알려 드리니, 그 경로에서 손으로 옮기시면 됩니다.

> ⚠ **갈아 끼우는 동안에는 창을 닫지 말아 주십시오.** 그 중간에 편집기가 꺼지면 되돌리기가
> 돌지 못합니다. 그때는 폴더 **옆에** `.zalkera-stash-` 로 시작하는 숨은 폴더가 남고 원래 소스가
> 그 안에 있습니다 — 확장이 그것을 찾아 주지는 못하니 직접 보셔야 합니다.

### 조용히 어긋나는 것

**폴더마다 자기 사이트를 기억합니다.** 그래서 사이트를 바꾸는 것은 **폴더를 바꾸는 일**입니다.
사이드바에서 다른 사이트를 고르면 지금 폴더는 그대로 두고, 어떻게 갈지 고르는 화면을 드립니다.

상태바 왼쪽 아래에 **지금 작업 사이트**가 보입니다. **올리기 전에 여기를 한 번 보십시오.**
⚠ 미리보기가 도는 동안에는 그 자리를 미리보기가 씁니다 — 그동안은 사이트 이름도 경고색도
안 보입니다.

### 미리보기는 진짜 데이터를 봅니다

미리보기는 **상용 데이터를 읽습니다.** 화면에 보이는 상품·주문은 진짜입니다.

자리를 비우실 때는 **미리보기 중지** 를 눌러 주십시오. 중지는 내 컴퓨터의 서버를 멈춥니다 —
서버 열쇠는 유효기간(최대 12시간)까지 살아 있으니, 지금 없애시려면 **로그아웃**하십시오.

### AI 연결이 여는 것

「AI 연결」을 켜면 쓰시는 AI 가 **잘커라 서버에 붙습니다.**

| | AI 가 할 수 있게 되는 것 |
| --- | --- |
| **내 데이터 보여 주기** | 상품·글·설정 **조회** · 소스 **읽기·고치기** · **새 버전 발행** · **이전 판으로 되돌리기** |

⚠ **발행은 손님이 보는 사이트를 바꿉니다.** AI 에게 「올려 줘」라고 말씀하시면 그 자리에서
바뀝니다 — 확인을 한 번 더 묻지 않습니다. 무엇이 바뀌는지 먼저 보시려면 **미리보기**로 확인한
뒤에 말씀하십시오. 잘못 나갔으면 사이드바 **「버전 전환」**에서 이전 판으로 돌아가시면 됩니다.

이 문을 안 여셔도 사이트는 그대로 굴러갑니다. **「내려받기」·「배포」·「버전 전환」**은 사이드바에
그대로 있고, 그쪽은 사장님이 직접 누르시는 자리입니다.

🔴 **AI 가 서버에서 고쳐 둔 것이 있으면 올리기가 막힙니다.** 그 편집은 발행 전 상태로 서버에
쌓이고, 그 동안 **「새 버전 배포」도 「버전 전환」도 거절됩니다.** 처분하는 길이 둘입니다 — 그
편집을 새 버전으로 **발행**하거나, 편집을 **되돌리기**. 잘커라 콘솔의 「사이트 소스」 화면에서,
또는 사이트에 연결한 AI 에게 말해서 하실 수 있습니다. 사이드바는 그 상태를 미리 보여 주지
않습니다 — 누르셔야 알게 됩니다.

⚠ **AI 확장을 껐다 켜야** 반영됩니다. 켠 직후에 안 보이는 것은 고장이 아닙니다.

⚠ **「내 데이터 보여 주기」는 지금 Claude Code 형식으로만 적어 드립니다**(폴더의 `.mcp.json`).
Codex 로 그 연결을 쓰시려면 손으로 설정하셔야 합니다. 소스를 읽고 고치는 것은 둘 다 됩니다.

---

## 4. 실제 시나리오

### 시나리오 A — 대행사가 새 판을 보내왔다 (가장 흔합니다)

여러 사이트를 맡고 계시고, 그중 `nasiajai` 의 새 소스 zip 을 받으셨습니다.

```
① 사이드바 → 사이트 전환 → nasiajai
   그 사이트 폴더를 받아 두셨으면 「폴더 열기」를 권해 드립니다. 누르십시오.
   그 폴더가 딛는 버전이 서버와 다르면 두 번호를 함께 보여 드립니다.

② 상태바 왼쪽 아래를 확인            ← nasiajai 인지 눈으로 봅니다

③ 사이드바 → 작업 폴더 → zip 으로 교체 → 받은 zip 고르기
   확인 창이 뜹니다. 세 가지를 보십시오.
     · 출처 표시     — 이 zip 이 어느 사이트 것으로 «표시»돼 있는가
     · 폴더 경로     — 갈아 끼울 자리
     · 그대로 두는 것 — 남는 파일 목록

④ 미리보기 시작 → 화면 확인
⑤ 배포 전 검사
⑥ 새 버전 배포
```

**③의 「출처 표시」가 무엇을 말하나**

| 보이는 것                                             | 뜻                                 | 하실 일                                       |
| ----------------------------------------------------- | ---------------------------------- | --------------------------------------------- |
| 이 사이트에서 내보낸 소스로 **표시되어** 있습니다     | 표시가 이 폴더의 사이트와 같습니다 | 그대로 진행                                   |
| ⚠ **다른 사이트**에서 내보낸 것으로 표시되어 있습니다 | 표시가 다릅니다                    | **멈추고 확인하십시오.** 버튼 문구도 바뀝니다 |
| 출처 표시 **없음** — 도구는 알 수 없습니다            | 표시가 아예 없습니다               | 파일 이름과 보낸 곳으로 직접 확인             |

> **「표시 없음」은 흔하고 정상입니다.** 시작 소스 팩에서 만든 소스에는 표시가 없습니다.
> **없다고 잘못된 zip 이라는 뜻이 아닙니다** —
> 도구가 대신 봐 줄 수 없으니 사람이 본다는 뜻입니다.
>
> 표시는 **선언이지 증명이 아닙니다.** 「일치」가 떠도 그건 「그렇게 적혀 있다」까지입니다.

### 시나리오 B — 사이트 셋을 연달아 갱신한다

zip 을 세 개 받으셨습니다. **한 번에 하나씩** 하십시오 — 시나리오 A 를 사이트마다 반복합니다.

```
사이트 전환 → A  →  zip 으로 교체 → 미리보기 → 검사 → 올리기
사이트 전환 → B  →  zip 으로 교체 → 미리보기 → 검사 → 올리기
사이트 전환 → C  →  zip 으로 교체 → 미리보기 → 검사 → 올리기
```

**미리보기는 한 번에 하나만 돕니다.** 다음 사이트로 넘어가기 전에 중지하지 않으셔도 되지만,
갱신할 때 확장이 알아서 멈춥니다.

**섞이기 쉬운 자리**: zip 파일 이름이 `source.zip`·`site.zip` 처럼 비슷하면 헷갈립니다.
받으실 때 `nasiajai-20260823.zip` 처럼 **사이트 이름과 날짜를 넣어 두시면** 확인 창에서 대조가
쉽습니다. 넘기시는 쪽 명세(`spec/storefront-spec.md` §15)가 그 이름으로 보내라고 요구합니다 —
같은 규약이라 양쪽이 맞물립니다.

### 시나리오 C — 새 사이트를 시작한다

```
① 사이트 선택 → 그 사이트
② 예제 zip 다운로드 — zip 파일 하나가 저장됩니다   (받은 zip 이 이미 있으면 건너뜁니다)
③ zip 으로 시작 → 그 zip 과 「빈 폴더」를 고릅니다 — 있는 파일을 덮지 않습니다
④ 폴더 열기 → 사이트에 연결 → 그 사이트에 붙입니다
⑤ 미리보기 → 배포 전 검사 → 새 버전 배포
```

첫 납품에는 **출처 표시가 없습니다.** 정상입니다 — 아직 어느 사이트에서도 나온 적이 없으니까요.
한 번 올리고 나면 그 뒤로 우리가 내보내는 소스에는 표시가 붙습니다.

### 시나리오 D — 대행사에게 지금 소스를 넘긴다

**zip 으로 내보내기** 를 쓰십시오. 손으로 압축하지 마십시오 — 미리보기 열쇠와 자격증명이
딸려 갑니다. 내보내기는 그것들을 빼고 묶고, **이 폴더가 어느 사이트 것인지 표시를 찍어** 줍니다.
대행사가 그 zip 을 **범용 압축 도구로 풀어** 고친 뒤 다시 묶으면 표시가 따라와 시나리오 A 의
③에서 「일치」로 보입니다. 잘커라 확장으로 풀었거나 처음부터 새로 만들었다면 표시가 없습니다 —
그때는 「표시 없음」이 뜨고, 그게 정상입니다.

### 시나리오 E — 올린 뒤에 잘못된 걸 알았다

```
버전 이력  → 어느 판이 있는지 봅니다
버전 전환  → 어느 판을 켤지 고릅니다(되돌리기도 여기서)
```

올려 둔 판은 남아 있으니 되돌릴 수 있습니다.

### 시나리오 F — 개시한 사이트를 검색에 알린다

사이트가 `robots.txt` 와 `sitemap.xml` 을 내면, 남은 일은 검색 엔진 콘솔 세 곳에 **등록 → 소유 확인 → 사이트맵
제출**을 하는 것입니다. 세 곳의 순서는 같고 소유 확인 방법만 다릅니다. 대표 주소가 정해진 뒤 한 번 하면 되고,
주소를 바꾸면 다시 합니다.

**등록할 주소는 콘솔 「커스텀 도메인」에서 대표로 세운 그 주소**입니다. 플랫폼 주소(`*.zalkera.com`)는 검색
색인을 막아 두었으므로 그 주소를 등록하면 색인되지 않습니다. 아래에서 `$B` 는 그 주소이고 `https://` 까지
포함합니다 — 예: `B="https://www.example.com"`.

먼저 사이트가 무엇을 내는지 보십시오(잘커라 시작 팩에서 만든 사이트는 둘 다 냅니다. 직접 만든 소스라면 여기서
확인하십시오).

```bash
B="https://www.example.com"          # 대표 도메인으로 바꾸십시오
UA="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36"
curl -sA "$UA" "$B/robots.txt"
# Disallow: 에 /api 가 있는지 봅니다(쇼핑몰 팩은 장바구니·결제 경로도 함께 막습니다)
curl -sA "$UA" "$B/sitemap.xml" | grep -o '<loc>[^<]*</loc>'
# 홈이 있고(시작 팩이면 /policies 도), 상품·글·카테고리가 있으면 그만큼 늘어납니다. localhost 로 시작하면 대표 도메인부터 확인하십시오
```

맨 `curl` 은 방화벽에 막혀 403 이 날 수 있어 `UA` 를 붙였습니다. 브라우저 주소창에 `$B/robots.txt` 를 넣어 열어도
같은 것을 봅니다. `robots.txt` 앞부분은 Cloudflare 가 붙이는 관리 블록입니다(검색 크롤러 허용 · AI 학습 크롤러
차단). 사이트가 낸 줄은 그 뒤에 옵니다.

| | 등록 | 소유 확인 | 사이트맵 제출 |
| --- | --- | --- | --- |
| **Google Search Console** | 속성 추가 — 「도메인」 유형(www 유무·http/https 를 한 번에 덮습니다) | **DNS TXT 레코드**(이 유형은 DNS 확인만 받습니다) | Sitemaps 메뉴에 `$B/sitemap.xml` |
| **네이버 서치어드바이저** | 웹마스터 도구 → 사이트 등록 — `$B` 그대로 | **HTML 태그** — 잘커라 콘솔 「환경변수」에 확인값 등록(아래) | 요청 → 사이트맵 제출에 `$B/sitemap.xml` |
| **Bing 웹마스터 도구** | Google Search Console 에서 **가져오기** | 가져오기면 생략. 안 쓰면 Bing 이 주는 방식(XML 파일·메타 태그·CNAME) 중 하나 | 가져오기가 사이트맵도 가져옵니다 |

권하는 순서는 **구글 → Bing → 네이버**입니다. 구글을 DNS 로 확인하면 Bing 은 가져오기로 끝나고, 잘커라 콘솔을
만지는 곳은 네이버 하나만 남습니다.

**구글 — DNS 레코드.** Search Console 이 주는 `google-site-verification=…` 값을 도메인의 DNS 에 TXT 레코드로
넣고 반영을 기다린 뒤(`dig +short TXT example.com` 에 보이면) 「확인」을 누르십시오. 소스도 배포도 건드리지
않습니다. DNS 를 만질 수 없다면 속성을 「URL 접두어」 유형으로 따로 만드십시오 — 그 유형만 HTML 파일 확인이
열리고, 대신 www 유무·http/https 를 각각 등록해야 합니다. HTML 파일은 소스의 `public/` 폴더에 넣고 새 판을
올려야 하므로(시나리오 A~C) 손이 더 갑니다.

**네이버 — 환경변수.** 서치어드바이저가 주는 `<meta name="naver-site-verification" content="…">` 의 `content`
값을 잘커라 콘솔 「환경변수」에 이 이름으로 넣으십시오.

```
NEXT_PUBLIC_NAVER_SITE_VERIFICATION = <content 값>
```

저장하면 사이트가 다시 빌드됩니다(업로드 사이트는 저장 알림의 「빌드 기록」으로 진행을 볼 수 있습니다).
끝난 뒤 아래가 한 줄을 내면 서치어드바이저에서 「소유확인」을 누르십시오. 값을 소스에 직접 적지 않습니다 —
소스는 대행사·다른 사이트로 흘러가지만 환경변수는 이 사이트에만 붙습니다.

```bash
curl -sA "$UA" "$B/" | grep -o '<meta name="naver-site-verification"[^>]*>'
# 한 줄. 403 이 아닌데 0 줄이면 재빌드 전이거나 변수 이름 오타입니다
```

그다음 「요청 → 사이트맵 제출」에 `$B/sitemap.xml` 을, 「검증 → robots.txt 검증」에서 검색 크롤러가
허용(`User-agent: *` 아래 `Allow: /`)으로 읽히는지 보십시오.

**Bing — 가져오기.** Bing 웹마스터 도구에서 「Google Search Console 에서 가져오기」를 고르고 구글 계정을 연결하면
확인된 속성과 사이트맵이 함께 넘어옵니다. Bing 색인을 참조하는 AI 검색이 있어 챙겨 둘 만합니다.

**제출 뒤에 볼 것** — Search Console 「Sitemaps」의 「성공」과 발견된 URL 수 · 「URL 검사」에 `$B/` 를 넣어
「색인 생성 요청」 · 서치어드바이저 「요청 → 사이트맵 제출」의 처리 상태 · Bing 「Sitemaps」의 「성공」. 색인 자체는
며칠 걸리므로 콘솔에 「성공」이 떴는데 검색 결과에 아직 없는 것은 정상입니다.

**그 뒤에는** — 상품·글은 `sitemap.xml` 이 한 시간 안에 다시 만들어질 때 실립니다(`src/app/sitemap.ts` 의
`export const revalidate = 3600`). 소스로 만든 페이지는 새 판을 올리면 실립니다. 소스에 손으로 만든
라우트(`/contact` 같은)는 실리지 않으니 그때는 알려 주십시오. 도메인을 바꾸면 세 콘솔 모두 새 주소로 다시
등록하고 소유 확인도 다시 합니다.

**GA4 는 별개입니다.** 애널리틱스는 사이트맵을 받지 않습니다. GA4 관리 → 데이터 스트림 추가로 받은 측정 ID
(`G-…`)를 콘솔 「환경변수」의 `NEXT_PUBLIC_GA4_ID` 에 넣으면 재빌드 뒤 페이지마다 태그가 실립니다.
`G-` 로 시작하는 형식이 아니면(예: `GTM-…` 컨테이너 ID) 아무것도 실리지 않습니다.

```bash
curl -sA "$UA" "$B/" | grep -o 'googletagmanager.com/gtag/js?id=[A-Z0-9-]*'
# 한 줄이면 됩니다
```

| 증상 | 먼저 볼 것 |
| --- | --- |
| curl 이 403 · 「Sorry, you have been blocked」 | UA 없이 친 것입니다. 위 `UA` 를 붙이거나 브라우저로 여십시오 |
| Search Console 이 「소유권을 확인할 수 없음」 | DNS TXT 전파 전입니다. `dig +short TXT example.com` 에 값이 보일 때까지 기다리십시오 |
| 네이버가 메타 태그를 못 찾음 | 위 curl 이 403 이 아닌데 0 줄이면 재빌드 전이거나 변수 이름 오타입니다. 홈에서 확인하면 됩니다 |
| 사이트맵이 「가져올 수 없음」 | `curl -sA "$UA" "$B/sitemap.xml"` 이 200 인지, `<loc>` 이 `localhost` 가 아닌지 보십시오 |
| 플랫폼 주소(`*.zalkera.com`)로 등록했다 | 그 주소는 색인이 막혀 있습니다. 대표 도메인으로 다시 등록하십시오 |
| GA 태그가 안 실린다 | `NEXT_PUBLIC_GA4_ID` 값이 `G-` 로 시작하는지, 저장 뒤 재빌드가 끝났는지 보십시오 |

---


## 5. git · GitHub 와 함께 쓰기 (개발자·대행사)

이 장은 **git 을 아는 분**을 위한 것입니다. 사장님이 직접 고치시는 경우에는 안 읽으셔도 됩니다 —
앞 장까지가 전부입니다.

소스의 변경 이력을 GitHub 에 두고 싶으시면 **VS Code 에 이미 있는 것**을 쓰십시오 — 왼쪽의
소스 제어(Source Control) 뷰가 기본 Git 이고, GitHub 로그인과 「GitHub에 게시」도 들어 있습니다.
PR·리뷰까지 편집기 안에서 하시려면 확장 탭에서 `GitHub Pull Requests` 를 더 까십시오.
**잘커라 확장은 git 을 모른 채로 그 옆에서 돕니다.** 이 장은 둘이 어디서 만나고 어디서 안
만나는지를 적습니다.

### 누가 무엇을 하나

| | 하는 일 | 안 하는 일 |
| --- | --- | --- |
| **잘커라 확장** | 소스 받기 · 미리보기 · 새 버전 배포 · 버전 전환 · 확인 창에 git 상태 한 줄 · 배포 뒤 「git 태그 만들기」 단추 · `.git/info/exclude` 한 줄 | 누르지 않은 git 명령 · GitHub 접속 · 커밋·푸시 · 누르지 않은 태그 만들기 |
| **VS Code 소스 제어**(기본 Git) | 커밋 · 브랜치 · 차이 보기 · 되돌리기 · stash | 사이트 배포 |
| **GitHub 확장 + GitHub 로그인** | 푸시 · PR · 리뷰 · GitHub 에 게시 | 사이트 배포 |

**이력이 둘입니다.** 둘은 다른 물음에 답합니다.

| | 답하는 물음 | 단위 |
| --- | --- | --- |
| **버전**(잘커라 · 사이드바 「버전」·「버전 이력」) | 지금 손님이 보는 것이 무엇이고 무엇으로 되돌릴 수 있나 | 배포 한 번(폴더 전체) |
| **커밋**(git) | 소스가 어떻게 바뀌어 왔나 | 파일 변경 |

> **「새 버전 배포」가 올리는 것은 커밋이 아니라 «지금 폴더의 파일»입니다.** 커밋하지 않은 변경이
> 있으면 그것도 같이 올라가고, `.gitignore` 로 git 이 무시하는 파일(`public/uploads/` 같은)도
> 올라갑니다 — 그런 파일은 태그가 가리키는 커밋에 없습니다. 그래서 「이 커밋이 지금 라이브다」라고
> 말하려면 **커밋한 뒤에 배포**하셔야 합니다. 이 장의 순서는 전부 그 한 문장에서 나옵니다.

### 처음 한 번 — 레포 만들기

**소스는 있는데 git 이 아직 없을 때** (「소스 다운로드」로 받은 폴더 · 대행사에게 받은 zip 을 푼 폴더)

```
① 소스 제어 뷰 → 「리포지토리 초기화」(Initialize Repository)
② .gitignore 확인(아래 표)
③ 첫 커밋
④ 소스 제어 뷰 → 「GitHub에 게시」(Publish to GitHub) → private 를 고르십시오
```

**GitHub 에 레포가 이미 있을 때** (동료가 만들어 둔 레포를 이어받는 경우)

```
① 명령 팔레트 → 「Git: 복제」(Git: Clone) → 폴더를 고르고 엽니다
② 잘커라 사이드바 → 「사이트에 연결」 → 이 소스가 어느 사이트 것인지 알려 줍니다
   (레포에 .vscode/settings.json 이 함께 커밋돼 있으면 이 단계는 이미 돼 있습니다 —
    사이드바 위에 사이트 이름이 보이면 건너뜁니다)
③ 미리보기 시작
```

**`.gitignore` 에 꼭 있어야 하는 줄.** 잘커라 시작 소스 팩으로 만든 소스에는 이미 들어 있습니다.
직접 만드신 소스라면 넣으십시오.

```
.env
.env.*
!.env.example
.zalkera/source.json
node_modules
.next
```

확장이 `.gitignore` 에 손대는 줄은 **하나**입니다 — 미리보기를 켤 때와 「새 버전 배포」를 누를 때
`.env.local` 줄을 보장합니다(`.gitignore` 가 있으면 그 줄만 더하고, 없으면 폴더에 `.git` 이 있을 때만
만듭니다). `git init` 뒤 첫 배포에서는 그래서 「커밋하지 않은 변경 1개」(`.gitignore`)가 뜹니다 —
커밋하고 다시 누르시면 됩니다. 취소하셔도 그 줄은 남습니다.
그 밖에는 `.gitignore` 를 만지지 않습니다. `.zalkera/source.json` 은 `.gitignore` 가 아니라
`.git/info/exclude` 에 적습니다 — **이 폴더가 레포의 맨 위이고 `.git` 이 폴더일 때만**입니다. 한 레포
안에 여러 폴더를 두셨거나 워크트리(`.git` 이 파일)라면 위 표대로 직접 넣으십시오.

**커밋해도 되는 것과 안 되는 것**

| 파일 | 커밋 | 왜 |
| --- | --- | --- |
| 소스 전부 · `.gitignore` · `.github/workflows/ci.yml` | 됩니다 | 소스입니다 |
| `.vscode/settings.json` (`zalkera.tenant` 한 줄) | **이 레포가 그 사이트 전용이면** 됩니다 | 어느 사이트인지만 적혀 있습니다. 여러 사이트에 돌려 쓰는 템플릿 레포면 넣지 마십시오 — 받는 사람의 편집기가 그 사이트로 잡힙니다 |
| `.mcp.json` · `AGENTS.md` · `CLAUDE.md` | 됩니다 | 열쇠가 안 들어 있습니다(각자 로그인해서 씁니다) |
| `.env.local` 과 `.env` 로 시작하는 파일 | **안 됩니다** | 미리보기 열쇠가 들어 있습니다 |
| `.zalkera/source.json` | **안 됩니다** | 이 컴퓨터가 어느 버전에서 왔는지 적어 둔 기록입니다. 남의 것이 커밋을 타고 넘어오면 사이드바 「버전」이 남의 상태를 내 것으로 말합니다 |
| `node_modules` · `.next` | 안 됩니다 | 다시 만들어집니다 |

### 매일 — 고치고, 커밋하고, 배포하기

```
① 고친다 (직접 · AI)
② 미리보기 시작 → 화면 확인
③ 커밋                                  ← 소스 제어 뷰
④ 배포 전 검사 → 새 버전 배포            ← 잘커라 사이드바
⑤ 배포가 끝나면 알림에 「버전 N」이 나옵니다 — 그 번호를 남깁니다
     (단추가 안 떴으면 터미널에서 git tag zalkera/내사이트/v5 · 또는 커밋 메시지에 "버전 5")
⑥ 푸시 (동기화)                          ← 소스 제어 뷰
```

- **③ 이 ④ 앞입니다.** 위 「누가 무엇을 하나」의 마지막 문장이 이유입니다. 배포 확인 창의 맨 아래에
  `git: main @ 1a2b3c4 · 커밋하지 않은 변경 3개` 처럼 지금 git 상태가 한 줄 적힙니다 — 「깨끗함」이
  아니면 커밋하고 다시 누르십시오. 막지는 않습니다.
- **⑤ 는 단추 하나입니다.** 깨끗한 상태에서 배포하셨으면 배포 완료 알림에 **「git 태그 만들기」**
  단추가 뜹니다. 누르면 `zalkera/내사이트/v5` 태그가 **배포한 커밋**에 생깁니다(누르지 않으면
  아무것도 안 씁니다 · 커밋하지 않은 변경이 있었으면 단추가 안 뜹니다 · 배포 뒤에 커밋을 옮기셨으면
  눌러도 만들지 않고 손으로 찍는 명령을 알려 드립니다 — 그 태그는 라이브가 아닌 커밋을 가리키기
  때문입니다). 태그는 내 컴퓨터에만 생기니, 같이 올리시려면 소스 제어 뷰의 「태그 푸시」(Push Tags)를
  쓰거나 설정 `git.followTagsWhenSync` 를 켜 두십시오.
- **버전 번호는 세 곳에 있습니다** — 배포 완료 알림 · 사이드바 「버전」의 서버 줄 · 「버전 이력」.

**브랜치.** 배포는 **열려 있는 폴더**를 올립니다. 브랜치를 바꾸면 폴더의 파일이 바뀌고, 그 상태로
「새 버전 배포」를 누르면 **그 브랜치가** 라이브가 됩니다. 규칙을 하나 두시길 권합니다 —
**`main` 에서만 배포한다.** 누르기 전에 상태바 왼쪽의 브랜치 이름을 보십시오.

> `git pull`·브랜치 전환·stash 처럼 편집기 밖에서 파일이 바뀌어도 사이드바 「버전」이 따라옵니다 —
> 「확인 중」이 잠깐 떴다가 새 결론이 옵니다. 저장했을 때와 같은 셈입니다.

### 서버가 앞섰을 때 — 「서버 판으로 교체」 뒤에 차이를 본다

남이 콘솔에서 올렸거나, AI 가 새 버전을 발행했거나, 다른 컴퓨터에서 배포했으면 사이드바가
「서버가 더 최신」을 말합니다. git 을 쓰시면 이 상황이 오히려 편합니다.

```
① 커밋 (또는 stash)                    ← 지금 고치던 것을 먼저 챙깁니다
② 잘커라 사이드바 → 작업 폴더 → 서버 판으로 교체
③ 소스 제어 뷰를 봅니다 — 서버 버전과 내 마지막 커밋의 차이가 파일마다 뜹니다
④ 살펴본 뒤 커밋 — 예: "서버 버전 7 받음"
```

- **①을 건너뛰지 마십시오.** 교체는 커밋하지 않은 변경을 지웁니다. git 이 지켜 주는 것은
  **커밋한 것뿐**입니다. 확인 창 맨 아래의 `git:` 줄이 「커밋하지 않은 변경 N개」라고 하면 취소하고
  먼저 커밋하십시오. 「zip 으로 내보내기 먼저」는 git 을 안 쓰시는 분을 위한 단추이고, git 을
  쓰시면 커밋이 그 자리입니다.
- **③에서 `.github/workflows/` 아래 파일 두 개가 «삭제»로 뜹니다.** 서버가 그 폴더를 저장하지
  않아서, 서버 판으로 갈아 끼우면 그것이 없어집니다. 소스 제어 뷰에서 그 항목만 「변경 내용 취소」
  (Discard Changes)로 되살리십시오 — 되살릴 것은 `ci.yml` 하나면 됩니다(아래 「GitHub Actions」).
- **`.gitignore` 도 서버 판의 것으로 바뀝니다.** 직접 넣어 두신 줄이 있었다면 ③에서 차이로
  보이니 그때 되살리십시오. `.git` · `.vscode` · `.env.local` 은 교체가 건드리지 않습니다.

### 되돌리기는 둘입니다

| 되돌리는 것 | 누르는 것 | 바뀌는 것 |
| --- | --- | --- |
| **손님이 보는 사이트** | 잘커라 「버전 전환」 → 이전 판 | 라이브만. 폴더도 git 도 그대로입니다 |
| **내 폴더** | `git checkout zalkera/내사이트/v3` (태그를 남겨 두셨다면) · 또는 「서버 판으로 교체」 | 폴더만. 라이브는 그대로입니다 |

둘은 따로 갑니다. 라이브를 3번으로 돌리셨으면 폴더도 3번에 맞추고 나서 작업하십시오 — 안 맞추면
사이드바가 「로컬이 더 최신」을 말하고, 다음 배포가 5번 내용을 도로 올립니다. 폴더를 맞추는
가장 확실한 길은 「서버 판으로 교체」입니다(태그 커밋과 서버 판은 소스가 같지만, 서버는
`.github/workflows/` 를 저장하지 않으니 그 자리만 다릅니다).

### 여러 사람이 고칠 때 · GitHub Actions

- **합치는 자리는 GitHub 입니다.** 브랜치·PR·리뷰는 그쪽에서 하시고, 잘커라는 **마지막에 누가
  배포했는지**만 압니다. 규칙 둘이면 충분합니다 — `main` 에서만 배포한다 · 배포한 사람이 PR 이나
  커밋에 버전 번호를 남긴다.
- **푸시는 배포가 아닙니다.** GitHub 에 올려도 사이트는 안 바뀝니다. 사이트를 바꾸는 것은
  「새 버전 배포」(또는 콘솔의 zip 업로드)뿐이고, 푸시하면 자동으로 배포되는 장치는 **없습니다.**
- **시작 소스 팩의 `.github/workflows/ci.yml`** 은 GitHub 에 올리면 푸시마다 검사·타입 확인·빌드를
  돌립니다. 「배포 전 검사」보다 넓고(빌드까지 합니다) 느립니다. **잘커라 배포는 이 결과를
  기다리지도 읽지도 않습니다** — 빨간불이어도 「새 버전 배포」는 됩니다. 순전히 여러분의 검사입니다.
- 같이 실려 오는 `client-upgrade.yml` 은 특정 이름의 브랜치에서만 돌게 되어 있고, 그 브랜치를
  만드는 것이 지금은 없습니다. 지우셔도 됩니다.

### 확장이 하는 것과 하지 않는 것 — 이 장의 정직 고지

하는 것은 넷입니다.

- 「zip 으로 교체」·「서버 판으로 교체」·「새 버전 배포」 확인 창 맨 아래에 git 상태를 한 줄 적습니다.
  막지 않습니다. VS Code 의 Git 확장이 읽어 준 것을 옮기는 것이라, 그 확장이 꺼져 있거나 폴더가
  레포가 아니거나 상태를 못 읽으면 줄이 없습니다 — 낡은 값으로 「깨끗함」이라 적지 않습니다.
- 편집기 밖에서 바뀐 파일(git·AI·터미널)도 알아채서 사이드바 「버전」을 다시 셉니다. 이것은 git 과
  무관하게 늘 돕니다(확장이 폴더를 통째로 갈아 끼우는 동안과, 그 쓰기가 잠잠해진 뒤 2초까지는
  셈에서 뺍니다 — 그 사이에 다른 손이 고친 것은 다음 저장이나 변경 때 셉니다).
- 깨끗한 상태에서 배포하셨을 때만 완료 알림에 「git 태그 만들기」 단추를 둡니다. 누르셔야 쓰고,
  누르는 순간 배포한 커밋이 아니면 안 씁니다.
- 이 폴더가 레포의 맨 위이면 `.git/info/exclude` 에 `.zalkera/source.json` 한 줄을 적습니다.

하지 않는 것도 넷입니다.

- 커밋·푸시·브랜치를 만들지 않습니다. 누르지 않은 태그도 만들지 않습니다.
- GitHub 에 접속하지 않습니다. 토큰을 받지도 보관하지도 않습니다.
- `.gitignore` 는 `.env.local` 한 줄 말고는 만지지 않습니다.
- `.git` 폴더를 지우거나 갈아 끼우지 않습니다.

바뀌면 이 장과 변경 기록에 적습니다.

---

## 6. 잘 안 될 때

| 증상                                   | 먼저 볼 것                                                        |
| -------------------------------------- | ----------------------------------------------------------------- |
| 버튼이 회색이다                        | 그냥 눌러 보십시오 — 무엇이 필요한지 말해 드립니다                |
| 「이 폴더는 다른 사이트의 소스입니다」 | 상태바의 사이트와 폴더가 어긋났습니다. **이 폴더의 사이트로 돌아가기** 를 누르시면 폴더 쪽으로 맞춥니다 |
| 미리보기가 안 뜬다                     | **진단** 을 누르십시오 — npm·프록시·서버 연결·사이트 소스·의존성 설치를 봅니다. ⚠ 진단이 적는 Node 는 **편집기가 품은 것**이라 늘 ✅ 입니다. 이 컴퓨터에 낡은 Node 가 깔려 막히는 경우는 **진단으로 안 보이니** 터미널에서 `node -v` 로 보십시오 |
| 남이 올린 최신 소스로 내 폴더를 맞추고 싶다 | **서버 판으로 교체**. 「소스 다운로드」는 빈 폴더만 받습니다 |
| 「지난 갈아 끼우기가 끊긴 흔적」이 뜬다 | 그 폴더에 원래 소스가 있습니다. 옮기실 것을 옮기신 뒤 지우시면 알림도 멎습니다 |
| 갱신이 「비어 있지 않습니다」로 막힌다 | 「zip 으로 시작」을 쓰고 계십니다. **zip 으로 교체** 가 맞습니다. 빈 폴더였는데도 막히면 앞서 받다가 중간에 멈춘 파일이 남은 것이니 폴더를 비우고 다시 하십시오 |
| 갈아 끼운 뒤 미리보기가 느리다         | 두 「교체」 다 `node_modules` 를 지우므로 처음 한 번은 다시 받습니다. 정상입니다 |

**진단**(`잘커라: 진단`)이 대부분의 답을 줍니다. 그래도 막히면 출력 패널의 「잘커라」 채널
내용을 함께 보내 주십시오.

---

## 부록 A. 명령 전체 표

명령 팔레트(`Ctrl/Cmd + Shift + P`)에서 「잘커라」로 검색하면 전부 나옵니다. 사이드바에는 자주
쓰는 것만 있고, 「미리보기 다시 시작」처럼 팔레트에만 있는 것도 있습니다.

| 명령 | 하는 일 | 사이트가 바뀌나 |
| --- | --- | --- |
| **로그인** / **로그아웃** | 잘커라 계정 연결 · 해제(로그아웃은 미리보기 열쇠도 서버에서 폐기) | 아니오 |
| **사이트 선택** | 다룰 사이트를 고릅니다. 맡으신 사이트가 여럿이면 사이드바에서 「사이트 전환」으로 보입니다 | 아니오 |
| **소스 다운로드** | 배포 중인 소스를 **빈 폴더**로 받습니다 | 아니오 |
| **소스 zip 다운로드** | 같은 소스를 zip 파일 하나로 받습니다 | 아니오 |
| **예제 zip 다운로드** | 예제 사이트를 zip 파일로 받습니다 | 아니오 |
| **zip 으로 시작** | 받은 zip 을 **빈 폴더**에 풉니다 | 아니오 |
| **zip 으로 교체** | 지금 폴더의 소스를 받은 zip 으로 갈아 끼웁니다 — **되돌릴 수 없습니다** | 아니오 |
| **서버 판으로 교체** | 지금 폴더를 서버에 켜져 있는 판으로 갈아 끼웁니다 — **되돌릴 수 없습니다** | 아니오 |
| **zip 으로 내보내기** | 지금 폴더를 zip 하나로 묶습니다(열쇠·부스러기 제외 · 연결된 폴더면 출처 표시를 찍습니다) | 아니오 |
| **사이트에 연결** | 가지고 있는 폴더를 사이트에 잇습니다(파일은 안 움직입니다) | 아니오 |
| **작업 폴더 변경** | 이 창을 다른 폴더로 옮깁니다(소속은 안 바꿉니다) | 아니오 |
| **이 폴더의 사이트로 돌아가기** | 폴더와 사이트가 어긋났을 때 폴더 쪽으로 맞춥니다 | 아니오 |
| **미리보기 시작** / **중지** / **다시 시작** | 내 컴퓨터에서 사이트를 띄워 브라우저로 봅니다(상용 데이터를 읽습니다) | 아니오 |
| **배포 전 검사** | 올리기 전에 짚을 것을 알려 줍니다 — 막지 않습니다 | 아니오 |
| **새 버전 배포** | 지금 폴더를 올려 **사이트를 그것으로 바꿉니다** | **예** |
| **버전 전환** | 사이트를 다른 판으로 바꿉니다(되돌리기도 여기서) | **예** |
| **버전 이력** | 지난 버전을 봅니다 | 아니오 |
| **내 데이터 보여 주기** | 쓰시는 AI 를 잘커라 서버에 붙입니다 — 조회·소스 고치기·**발행**까지 열립니다 | AI 가 발행하면 **예** |
| **진단** | 안 될 때 무엇이 문제인지 봅니다 | 아니오 |
| **초기화** | 로그인·사이트 설정·미리보기 열쇠를 지웁니다(받은 소스는 안 지웁니다) | 아니오 |
| **도움말** | 사전 격 도움말을 엽니다 | 아니오 |

## 부록 B. 확장이 폴더에 쓰는 파일

| 파일 | 무엇 | 올릴 때 | git 에 |
| --- | --- | --- | --- |
| `.vscode/settings.json` | `zalkera.tenant` 한 줄 — 이 폴더가 어느 사이트인지 | 빠집니다 | 전용 레포면 커밋해도 됩니다 |
| `.zalkera/source.json` | 이 폴더가 어느 버전에서 왔고 그 뒤로 고쳤는지의 기준 | 빠집니다 | 커밋하지 마십시오 |
| `.env.local` | 미리보기 주소·열쇠 다섯 줄(직접 적으신 다른 줄은 그대로 둡니다) | 빠집니다 | **커밋하지 마십시오** — `.git` 이 있으면 미리보기·배포 때 확장이 `.gitignore` 에 보장합니다 |
| `.mcp.json` · `AGENTS.md` · `CLAUDE.md` | 「내 데이터 보여 주기」가 적는 AI 연결·규약(열쇠 없음) | `.mcp.json` 은 빠지고 나머지는 실립니다 | 커밋해도 됩니다 |
| `.gitignore` 의 `.env.local` 줄 | 미리보기를 켤 때와 「새 버전 배포」 때 `.git` 이 있으면 보장 | — | — |
| `.git/info/exclude` 의 `.zalkera/source.json` 줄 | 이 폴더가 레포 맨 위이고 `.git` 이 폴더이면, 버전 기록을 쓸 때 | — | 커밋되지 않는 파일입니다 |
| `.git/refs/tags/zalkera/…` | 「git 태그 만들기」를 **누르셨을 때만** | — | 「태그 푸시」로 올립니다 |
| `.zalkera-stash-*` (폴더 **옆**) | 갈아 끼우다 끊겼을 때 남는 원래 소스 | — | 레포 밖입니다 |

CLI(`@zalkera/cli`)를 함께 쓰시면 `.zalkera/sync.json` 이 하나 더 생깁니다 — CLI 가 스스로 git
에서 감춥니다(`.git/info/exclude`).

## 부록 C. 설정

| 설정 | 기본값 | 뜻 |
| --- | --- | --- |
| `zalkera.tenant` | (비움) | 이 폴더의 사이트 코드. 「사이트에 연결」·받기가 적습니다 |
| `zalkera.npm` | `bundled` | 의존성 설치에 쓸 npm — `bundled`(확장이 품은 것) · `system`(이 컴퓨터 것) · `auto`. **컴퓨터 단위**입니다 |
| `zalkera.apiBase` | 잘커라 서버 | 서버 주소. **컴퓨터 단위**입니다 — 소스 폴더의 설정으로는 못 바꿉니다 |

`zalkera.npm` 과 `zalkera.apiBase` 를 컴퓨터 단위로 둔 이유는 하나입니다 — **받아서 여는 소스가
「어떤 프로그램을 실행할지·어디로 접속할지」를 정하면 안 되기** 때문입니다.
